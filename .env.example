# Set these in your shell; the runner does not automatically load .env files.
CAPSOLVER_API_KEY=
# Optional local Ollama model already installed on your machine.
WAGECUCK_AGENT_MODEL=
WAGECUCK_AGENT_ENDPOINT=http://localhost:11434
